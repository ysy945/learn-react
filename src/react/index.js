import { createElement, cloneElement } from "./ReactElement";
import { Component, PureComponent } from "./Component";
import {
  useCallback,
  useEffect,
  useReducer,
  useRef,
  useState,
} from "./ReactHooks";

const React = {
  createElement,
  Component,
  PureComponent,
  useCallback,
  useEffect,
  useReducer,
  useRef,
  useState,
  cloneElement,
};

export default React;
